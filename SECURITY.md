# 安全政策

当前维护线为 0.14.x。报告漏洞时请使用 GitHub 仓库的 **Security → Report a vulnerability**（由维护者启用私密漏洞报告后可见）。若私密通道尚不可用，仅创建不包含漏洞细节和敏感数据的联系请求；等待维护者提供私密通道后再发送详情。

不得在公开 issue/PR 上传密码、私钥、数据库、master.key、完整备份、订阅令牌、客户数据或生产日志。提交重现步骤时使用合成数据。发现凭证泄露应轮换相关凭证，删除文件或改仓库可见性不能撤销已泄露的秘密。

发布目录从允许清单创建新历史，配合秘密扫描和文档检查；不自动带入原服务器配置、私人报告、截图或 Git 历史。构建依赖固定在锁文件和版本中，代理核心核验上游 SHA256。SHA256 校验不能替代对源码和发布身份的审查。

生产要求 HTTPS、回环内部端口、最小文件权限、独立服务账号与受限备份。无日志模式不是匿名或合规认证；第三方系统可能仍有记录。公开代理默认不应用于敏感业务。没有完整服务器权限隔离或组织多租户安全保证。

安全修复需附回归测试，验证失败关闭、不泄露凭证，以及旧版本迁移策略。仓库转公开前应启用 GitHub 私密漏洞报告、依赖告警、秘密扫描与分支保护；具体可用功能取决于 GitHub 账号套餐。
