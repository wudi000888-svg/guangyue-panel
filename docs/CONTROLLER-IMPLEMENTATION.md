# 0.16.0 主控站与业务站实施记录

Pro 支持主控站和轻量业务站两种运行角色；Lite 保留独立单机部署。业务站主动通过 HTTPS 拉取配置，不提供远程管理登录入口，不依赖远端 PostgreSQL/Redis。旧独立面板联邦接入保留为兼容功能。

## 数据与执行边界

- 主控维护用户、出口资源、业务站分组、资源分配、节点模板和统一订阅。
- 业务站保留独立 TLS / Reality 私钥、SQLite、配置版本和流量检查点，仅接收获授权的代理凭据与出口。
- 每站点保留 VLESS / HY2 默认直连节点；绑定出口创建附加节点，不替换默认节点。
- 有限总配额按业务站预分配，未消费的站点额度占用总余额；离线站点额度不自动重新分配。流量上报单调且幂等。
- 配置有授权有效期，业务站在短暂断连时使用最近生效配置，到期停止授权；不保证断网期间即时撤销。
- 全局出口支持独占/共享，资源更新按需下发；各业务站独立检测链路，业务节点不自动更换出口。
- 同站点同出口的节点共享质量报告与标签，协议测速独立。出口变化或报告过期后重新检测。
- Release 分别提供 Lite、Pro 安装包；Pro 包支持主控与业务站，包内标记决定默认安装角色。

## 验收门槛

- [x] 注册、身份固定、版本确认、限权配置同步、断连与过期控制。
- [x] 用户授权、出口分配、独占约束、变更下发、统一私有/公共订阅。
- [x] 配额分配、重复/乱序流量、撤销与离线边界。
- [x] 质量检测复用、IP 变化隔离、前端报告与标签一致。
- [x] 双版本安装包、业务站安装和升级、迁移/备份/回滚。
- [x] 多实例、真实核心 TCP/UDP、数据库、UI 与安装 CI。

具体证据和边界见 [发布验收记录](VALIDATION.md)。生产部署和正式发布属于独立的最终交付步骤，以对应 Release 的交付记录为准。
