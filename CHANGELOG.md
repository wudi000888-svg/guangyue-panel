# 变更记录

## 0.30.5 — 自动构建发布

- 由主分支提交自动生成；安装包、校验清单和 SBOM 随 Release 提供。

## 0.30.4 — 子站共享预算冷却与挂载节点组修复

- 子站挂载节点禁止进入默认本地节点组，管理员新建的节点组均可分配，历史非法归属自动迁移到默认子站节点组。
- 子站资源预算按站点共享；达到上限立即撤销所有成员订阅和远端授权，冷却时长可配置，期满自动清零并恢复，节点组归属保持不变。
- 增加预算暂停状态、冷却时间和恢复逻辑的前后端回归覆盖，版本同步更新为 0.30.4。

## 0.30.3 — 自动构建发布

- 由主分支提交自动生成；安装包、校验清单和 SBOM 随 Release 提供。

## 0.30.2 — 自动构建发布

- 由主分支提交自动生成；安装包、校验清单和 SBOM 随 Release 提供。

## 0.30.1 — 自动构建发布

- 由主分支提交自动生成；安装包、校验清单和 SBOM 随 Release 提供。
- 套餐编辑改为先选节点组，再从已选节点组内选择本站单独节点；后端拒绝跨组或无组的单独节点权限。

## 0.30.0 — 独立节点组管理与破坏性删除

- 节点组从套餐管理中抽出为独立入口 `/node-groups`，保留旧的 `?tab=groups` 访问方式。
- 左侧导航收敛为工作台、用户与套餐、节点与出口、系统与服务四个一级区域，原有页面和能力继续保留。
- 普通套餐与节点组支持管理员确认后直接物理删除；系统管理员套餐、演示套餐和默认节点组继续保护。
- 补充删除回归测试、国际化文案以及面板框架技术债和后续施工方案。

## 0.29.0 — 套餐队列与精确节点权限

- 移除用户独立配置入口，新建成员自动使用演示套餐，管理员只能通过套餐调整额度、有效期、协议和节点权限。
- 套餐支持同时选择节点组和单独节点；单独节点权限会进入订阅和节点授权判定，套餐页显示精确节点清单。
- 购买新套餐立即启用新套餐，并暂停当前套餐；新套餐到期后按购买顺序恢复上一套餐的剩余有效期、配额周期、协议和节点权益。
- 成员订阅页显示当前套餐、节点权限和排队套餐；套餐权限变更继续同步本地节点、挂载子站和业务站授权。
- 安装包、校验清单和 SBOM 随 Release 提供。

## 0.28.4 — 套餐统一决定节点权限

- 子站节点只需“保存挂载”，用户按已生效套餐包含的节点组自动获得节点和子站预算；移除“保存并分配用户”、手动选择用户和用户列表中的“分配节点”入口。
- 修复旧版个人节点组覆盖套餐的问题：套餐授权优先，不能再单独扩大或缩小套餐用户的节点权限。
- 修复旧版手动挂载记录阻止套餐用户自动授权的问题。更换套餐同步订阅与远端撤权，保留用量、周期、凭据以及未结算额度。
- 更新操作指引；新增两种历史挂载模式下的 VLESS/HY2 授权、混合订阅、撤回、计量及 PostgreSQL 回归。

## 0.28.3 — 修复默认子站节点组范围

- 升级时自动修正历史 `default-subsite` 节点组的订阅范围为 `subsite`，保留管理员自定义名称、描述、启用状态和排序。
- 将历史挂载节点中的本地/公共分组迁移到子站节点组，避免本地套餐继续授权直连子站节点。
- Lite/Pro 安装包、SHA256 校验清单与 SBOM 随 Release 提供。

## 0.28.2 — 本地节点与子站出口隔离

- “普通节点”统一改名为“本地节点”，左侧新增独立的“子站节点”入口。
- 子站挂载节点只能进入 `subsite` 范围的节点组，修复 HY2 等挂载节点被混入本地节点组和本地/混合订阅的问题；历史配置仍按原权限授权，但对外展示和新配置会自动隔离。
- 新增“子站 IP 池”：主站通过加密桥接中转，客户端仍连接主站，最终使用子站 IP 出口；它与客户端直连子站的“子站节点”完全分开。
- 子站节点页和子站 IP 池页分别提供挂载、创建中转出口、删除和状态刷新入口，节点组选择器只显示对应范围。
- Lite/Pro 安装包、SHA256 校验清单与 SBOM 随 Release 提供。

## 0.28.1 — 子站月度资源预算

- Pro 主站可为每个子站设置按 UTC 自然月计算的资源预算，单位 GiB，0 表示不限。
- 预算同时覆盖令牌连接的挂载节点和传统业务子站；主站实时累计子站上报流量，达到上限后停止下发新代理授权，次月自动清零恢复。
- 子站节点池配置页直接显示预算、当月用量和剩余额度，保存节点挂载策略时可一并更新预算。
- 增加预算持久化、流量累计、超限撤销和月份恢复回归覆盖。

## 0.28.0 — 自动构建发布

- 由主分支提交自动生成；安装包、校验清单和 SBOM 随 Release 提供。

## 0.27.0 — 子站节点分配与节点组识别简化

- 子站 IP 池改为单列表直接勾选挂载，默认加入子站节点组；保存后直接选择用户、预览并确认，无需先创建套餐。保留原有挂载分组与手动预算设置。
- 用户管理支持单人和批量“分配节点”，可追加、精确替换或恢复套餐/默认组；额度、有效期、协议、周期用量和账号凭据保持不变。空的精确选择撤销全部节点权限。
- 节点组与挂载列表显示协议、站点名称、入口地址、出口 IP 和同步状态；支持按来源和节点/站点/协议/IP 筛选，同名跨站、跨协议节点分别展示。
- 分配建议优先使用默认子站组，避免重复勾选已经覆盖同一节点的普通组；新增模型迁移阻止回退到忽略直接权限的旧版。
- Lite/Pro 安装包、SHA256 校验清单与 SBOM 随 Release 提供。

## 0.26.0 — 子站 IP 池与自动分组授权

- 私有 IP 池新增子站 IP 池，可导入管理令牌、选择共享节点并挂载到主站节点组，客户端仍直连子站真实节点。
- 新安装及升级预置空的“默认子站节点组”；不改变已有用户权限、节点分组或管理员自定义设置。
- 新挂载按主站用户/套餐的节点组自动授权，自动分配、预留和结算额度；旧手动配置保留在高级设置。
- 日常用户管理隐藏内部挂载代理身份，保留监控计量和子站本地独立运营。
- Lite/Pro 安装包、SHA256 校验清单与 SBOM 随版本一起发布。

## 0.25.0 — 子站节点挂载与统一订阅管理

- Pro 子站管理新增节点池：按节点或子站节点组选择，设置主站展示别名与节点组，分配成员和剩余站点预算；默认不挂载、不授权。
- 客户端直接连接子站原有 VLESS/HY2 节点，沿用子站出口；子站签发独立代理身份，主子站同名或同编号用户互不覆盖。
- 普通与公共订阅合并为“订阅管理”，支持全部、本站、公共、子站挂载来源；所有格式标注 `[挂载·站点名称]`，旧普通/公共订阅地址保持原范围。
- 节点组支持混合节点来源，保留原组 ID 和权限；公共采集、质量筛选和黑名单继续保留。
- Lite/Pro 配对令牌新增节点共享控制；短租约、用户撤销、共享暂停、令牌撤销和核心进程租约检查限制外来访问，保留子站本地运营。
- 挂载流量按子站倍率计量，以累计水位和不可变倍率版本校验、去重；跨站预算预留、周期结算、离线移除和令牌替换保留未结算账本。
- 核心重启等待新进程出现后再检查服务可用性，避免卸载授权时误用旧进程端口、重复重启并触发 systemd 启动限流。
- 主子站均须升级到本版后使用节点挂载。外来授权撤销可能重启代理核心并造成现有连接短暂重连；离线连接的额度须待最终用量确认后释放。
- 同步发布 Lite / Pro Linux amd64 安装包、SHA256SUMS 和 SBOM；加入真实协议、租约进程守护、PostgreSQL 与安装升级回归验证。

## 0.24.0 — 子站令牌直连与管理调度

- Pro 直接粘贴子站令牌建立连接，不再要求预建站点、注册文件或分组；永久令牌、旧 gyp_ 令牌和重复导入更新均受支持。
- 统一子站列表展示在线用户、节点、实时速率和累计流量，提供用户、节点、监控及远程任务入口，支持暂停和恢复代理服务。
- 在线、离线子站都可直接移除；子站本地管理员、数据、订阅和独立运行保留，可本地恢复服务或撤销令牌。
- 历史独立连接只迁移主站目录，不再打开页面就转换远端角色；旧拉取式子站保留权限和计量兼容路径。
- 新增持久化站点身份、凭据与权限隔离、重复接入、离线移除、服务启停及旧模式转换回归测试。
- Lite / Pro 同步提供 Linux amd64 安装包、SHA256SUMS 和 SBOM。

## 0.23.4 — Pro 统一站点模型

- Pro 不再把独立站点和托管子站拆成两套产品；历史联邦接入站点首次打开群站管理时自动转换为业务子站。
- 旧 Pro/Lite 站点通过管理令牌写入托管连接标记并自动重启，保留原站点登录、数据和代理核心配置。
- 保留旧联邦 API 作为迁移入口；新站点统一使用一次性业务连接令牌和主站权限、配额、节点及流量同步。

## 0.23.3 — 令牌连接托管子站

- 新建托管子站改用一次性连接令牌，不再要求注册文件；安装器支持 `--controller-url`、`--site-id` 和 `--connect-token`。
- 主站子站页显示连接令牌和安装参数；子站继续保留本地 owner、节点、订阅与租约内离线运行能力。
- 保留旧 `--enrollment-file` 和 `/api/business/enroll` 作为迁移兼容路径，新增 `/api/business/connect`。
- 新增令牌安装、连接交换和业务同步回归覆盖，更新 Lite/Pro 安装与配置文档。

## 0.23.2 — 永久有效绑定令牌

- 配对/接入令牌支持选择永久有效，后端以 `expires=0` 保存并允许网关长期校验。
- 令牌列表显示“永久有效”；创建表单在永久模式下禁用天数限制并提示仅在可信场景使用。
- 新增永久令牌的创建、列表、网关访问和撤销回归测试，更新 Lite/Pro 使用文档。

## 0.23.1 — 独立站配对与本地运行

- Lite 新安装默认 standalone，不再要求注册文件；Pro 新安装继续默认为 controller。
- 独立站登录后可生成和撤销配对令牌；Lite 只能提供令牌，Pro 可粘贴站点地址和令牌直接接管。
- 托管 Lite/Pro 子站保留本地 owner、登录、节点和订阅能力；本地 owner 不会上报到主站，主站策略仍控制托管成员与流量。
- 新增 Lite/Pro 配对和子站本地运行回归测试，更新安装、配置和发布文档。

## 0.23.0 — Lite 子站与 Pro 主站

- Lite 新安装默认作为 SQLite 子站，使用 Pro 主站生成的注册文件；Pro 新安装默认作为主站，兼容已有 Pro 业务站。
- 主站集中控制子站成员、协议许可、到期时间、配额、节点权限组、倍率和出口；子站不生成独立管理员，不开放管理接口。
- 群站页显示子站的 LITE / PRO 标识，注册弹窗提供对应 Lite 安装包下载及安装说明。
- 普通升级保留原角色；旧 Lite 继续独立运行。阻止托管子站在升级中意外改变站点身份、edition 或角色。
- 新增 Lite / Pro 子站生命周期、跨站权限隔离与撤销测试，以及 Lite 默认子站的真实 Linux 安装、TCP/UDP、升级和回滚验证。
- 安装默认值变化：新装独立 Lite 必须显式指定 `--role standalone`。已有站点不会自动合并用户或转为托管子站。

## 0.22.1 — 节点保存与自动刷新修复

- 添加或编辑节点成功后立即更新列表；后续列表请求暂时失败时保留已保存结果，并自动重试刷新。
- 代理核心配置应用导致保存响应中断时，按请求标识自动核对服务端结果，避免将已保存误报为 Failed to fetch。
- 新增节点请求支持去重，重试不会重复创建或覆盖后续编辑；节点与 VLESS 用户凭据原子提交，失败不会留下半成品。
- 保持旧版独立站点接口兼容；无法确认保存结果时明确提示，不将未知状态显示为成功。

## 0.22.0 — 客户端中心与实时用户监控

- 新增客户端中心：6 款常见客户端、5 类系统、26 个官方入口，按平台和架构选择；明确订阅格式、HY2 核心兼容性及 App Store 地区限制。
- 新增管理员实时监控：每用户 VLESS 活跃会话、HY2 认证 QUIC 连接、上传/下载速率与最近五分钟趋势，支持搜索、分页和单用户查看。
- 监控复用后台约 5 秒采样，未知数据保留缺口，核心重启/计数重置后预热；历史仅保存在有上限的内存中，不改变流量计费。
- 安装包包含固定 Xray 会话计数扩展、完整修改源码与 MPL 许可，正确处理多路复用；兼容旧更新器的校验流程，避免安装后被旧核心覆盖。
- 监控范围为当前站点本机核心；独立站点可切换查看，主控不汇总业务站连接。Lite/Pro 同步发布安装包、SHA256SUMS 与 SBOM。

## 0.21.5 — 自动构建发布

- 由主分支提交自动生成；安装包、校验清单和 SBOM 随 Release 提供。

## 0.21.4 — 工单提交稳定性修复

- 工单写入成功后立即保留返回的工单，详情或列表刷新暂时失败时不再把成功提交误报成失败。
- 为没有 `crypto.randomUUID()` 的嵌入式浏览器增加安全随机降级，保证幂等操作标识始终满足后端校验。
- 增加工单操作标识回归测试。

## 0.21.3 — 兑换码管理安全改进

- 管理员作废兑换码改为使用当前已认证会话，无需重复输入密码，支持批量作废。
- 新生成兑换码使用站点 master.key 加密保存明文，隐藏后可由管理员批量重新查看或下载；历史未保存明文的兑换码明确标记为不可恢复。
- 增加兑换码重新查看接口、权限校验、无缓存响应和前端中英文提示。

## 0.21.2 — 顶部账户余额

- 顶部常驻当前登录账号的可用余额，点击进入账户余额页；移动端、中英文和浅深色主题均支持。
- 兑换、人工调整及订单操作后自动刷新；可见页面每 30 秒同步，返回页面时重新获取。
- 管理员查看其他成员的账目不改变顶部身份；加载失败显示不可用状态，退出与站点切换取消旧请求。
- 修复窄屏英文仪表盘资源卡片底部按钮的横向溢出。

## 0.21.1 — 套餐价格与管理员余额操作

- 套餐管理可直接设置 CNY 价格，价格随套餐模板版本保存；销售配置新建时自动带入套餐价格。
- 已认证的管理员会话可直接调整成员余额，无需重复输入账户密码；原因、幂等操作标识和资金账本校验保持不变。

## 0.21.0 — 余额、兑换码、订单与工单

- 站内 CNY 余额与不可覆盖资金流水，管理员入账/赠送/扣减；整数分计算、并发保护、启动对账及快照校验。
- 定额兑换码支持批量生成、有限期/永久有效、手动作废与一次性兑换；列表只显示尾号，生成结果加密保存以支持安全重试。
- 套餐销售快照、唯一订单号、余额冻结/扣款/解冻、同版本续费、退款和持久化故障恢复；不接入外部支付。
- 普通用户图片工单、订单/节点关联、管理员内部备注、状态和优先级；PNG/JPEG 限制、独立受限处理进程、私有附件与容量回收。
- Lite/Pro 共用业务规则，主控统一记账、业务站撤权结算屏障、账户历史归档；无日志模式保留业务数据。
- 简易/专业模式保留核心账户功能，中英文和移动端界面；修复带订单参数的路由跳转。
- 数据库迁移 005；旧账号、凭据与用量保留，销售默认关闭。升级后直接回退旧数据模型受保护。

## 0.20.0 — 节点倍率与套餐权益

- 新增 0–100×节点倍率、普通/公共节点权限组、套餐分类与不可变套餐版本；管理员手动分配、续期、重置周期或停用，无支付系统。
- 删除节点自动移出所有分组，删除出口/来源级联清理绑定节点；保留最终计量水位与历史用量。
- 按实际流量与倍率配额分别统计，支持每 30 天/自然月周期；套餐变更和续期不会隐式清零。
- 订阅、单节点链接、二维码、质量报告与实际核心授权统一按组过滤；VLESS 撤权采用受控核心重启关闭已有连接。
- Pro 主控与业务站采用加权额度、稀疏用量确认与周期撤回；旧业务站须升级后才能使用新权限和倍率。
- 新增套餐管理、批量权益预览、节点批量倍率/分组、订阅用量明细，适配移动端、中英文、浅深色及简易模式。
- 存量迁移保留账号凭据、订阅和已有用量，现有节点按 1×初始化；迁移后禁止直接回退到 0.19.x 等旧数据模型。

## 0.19.2 — 最新版本徽标样式

- 当前版本已是最新时，左上角版本徽标使用跟随主题的中性灰色；仅有新版本可用时显示黄色提示。
- 浅色、深色、移动端和收起侧栏保持一致，收起侧栏时仍保留可见状态点。

## 0.19.1 — 重启时的任务恢复

- 修复任务刚被领取、尚未读取状态时遇到服务停止，任务可能永久停留在“运行中”的竞态。停止后重新排队，已取消的任务不会被恢复。
- 包含 0.19.0 的 BBR/HY2 优化、默认安装配置、模式入口去重与紧凑更新卡片。

## 0.19.0 — 网络优化与紧凑版本卡片

- 系统设置提供独立 BBR / HY2 优化开关、实际状态及执行结果；与界面模式和日志模式互不冲突。
- Lite、Pro 主控和业务站新安装默认启用 BBR/fq、至少 8 MiB UDP 缓冲和 HY2 标准 BBR 策略；升级保留已有选择。
- 固定 root 维护任务执行白名单操作；关闭恢复接管前的系统值，失败恢复原配置，与版本操作互斥。
- 左上角使用紧凑版本卡片，支持立即更新、选择版本、安装基线内回退和成功后 8 秒刷新。
- 桌面与移动端互斥渲染界面模式入口，修复重复展示；完善中英文及触控布局。

## 0.18.0 — 灵活密码与移动控制台

- 成员创建、自助改密、管理员重设支持 1 位起的密码，无复杂度要求；最多 72 字节，兼容现有 bcrypt 密码与版本回退。编辑成员时留空仍保持原密码。
- 统一控制台表面、字号、间距与操作密度。移动端采用单行顶栏、抽屉导航、账户工具菜单和按权限显示的常用底部导航。
- 节点和私有出口在手机上使用卡片与统一分页，支持全选；用户、业务站、独立站点和任务列表使用字段明确的卡片布局。
- 优化触控尺寸、弹窗滚动、安全区、中英文和浅深色主题；修复桌面收起导航后手机抽屉内容不全的问题。

## 0.17.0 — 在线更新与安装基线回退

- 左上角版本徽标、正式 Release 检查、更新版本选择与确认、发布说明和操作进度。
- 独立按需启动的 root 更新执行器，面板仍为非特权用户；仅接受固定仓库正式安装包，校验下载、文件清单和代理核心。
- 后台备份、升级及健康检查，成功后 8 秒自动刷新；中断操作具有独立恢复记录，重复提交幂等。
- 持久安装基线不可由页面降低；回退只切换兼容旧程序，保留当前账号、订阅和流量，数据库不兼容时拒绝。
- Lite / Pro 同时支持；增加真实 socket / systemd 更新和回退集成测试及完整操作文档。

## 0.16.0 — 主控站与业务站

- Pro 增加主控 / 轻量业务站角色：一次性注册、身份固定、配置拉取、租约过期撤销及状态回传。
- 统一成员授权、私有/公共订阅聚合、出口共享/独占分配、每站累计额度预留与幂等流量计量。
- 业务站配置 UI、分组、成员额度、附加 VLESS/HY2、远程检测与报告；原独立面板接入保留。
- 同机同出口 VLESS/HY2 共用质量报告及标签，24 小时成功缓存，测速结果仍独立。
- Release assets 拆分 Lite / Pro；Pro 业务站 SQLite，无 PostgreSQL/Redis 依赖；安装和升级按角色保留身份。
- 完整业务站安装、使用、离线与备份边界文档。有限配额采用预分配及周期采样，不提供即时零超额计费保证。


## 0.15.0 — Lite 与广月面板 Pro

- Lite 保留 SQLite；Pro 增加 PostgreSQL、Redis、独立站点 schema 与控制器租约。
- 增加持久化任务中心、错峰任务调度、重启恢复、配置版本和轻量状态轮询。
- Pro 支持站点接入、状态监测、管理切换、限权令牌和撤销；每台 VPS 独立运行代理核心。
- Vue Router 页面懒加载、Pinia 偏好与权限状态、统一列表分页、中英文界面。
- 增加双版本安装、SQLite→PostgreSQL 迁移、Pro 便携备份和数据库级升级回滚。
- 主许可改为 LGPL-3.0-only；保留历史 MIT 和第三方许可。
- 验证双版本 systemd 安装与回滚、现有站点 Pro 迁移及真实 VLESS/HY2 TCP/UDP；修复 PostgreSQL 流量汇总字段歧义。


## 0.14.0 — 开源准备版

- 品牌统一为“广月面板”，定位面向小型跨境电商团队的单 VPS 工作台。
- 将维护中的 Go/Vue 版本整理为独立源码树和全新 Git 历史；剔除服务器资料、部署状态、旧报告和凭证。
- 提供参数化安装预检查、Nginx 分流预览、证书成对更新、带离线备份的升级和故障回滚。
- 补齐中文安装/使用/运维/配置/架构文档及英文项目入口。
- 保留 VLESS Reality、HY2、多用户订阅、私有/公共池隔离、导入来源、DNS/IPv6、质量报告与双界面模式。
- 固定代理核心版本与下载校验，提供第三方许可证汇总、SPDX 依赖清单、CI 与草稿 Release 流程。

版本号沿用自维护 Go/Vue 版本线；它与原版 fake-ui 2.3.1 的版本号体系不同。当前发布不承诺旧 Python 架构数据库的直接迁移。
